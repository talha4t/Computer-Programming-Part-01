#include <stdio.h>

int main() {
    int n;

    n = 10;

    if(n >= 0) {
        printf("its a positive number:\n");
    }
    else {
        printf("the number is negative:\n");
    }

    return 0;
}